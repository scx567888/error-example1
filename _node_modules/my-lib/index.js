import component1 from './component1/index.vue'

//以下为组件
const components = [component1];

const ComponentInstaller = {
   install: (app) => {
      components.forEach(c => app.component(c.name, c));
   }
};

export {
   ComponentInstaller
}